# Deja Dup needs a monitor to run in the background
# My lubuntu 11.10 system doesn't automatically start it.
# This script starts it.

# usage:
# - open a terminal
# - run this script
# - leave the terminal open. The monitor will write to it periodically.
/usr/lib/deja-dup/deja-dup/deja-dup-monitor
